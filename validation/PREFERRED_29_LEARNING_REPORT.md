# 29 篇用户范本：本轮学习与落地记录

日期：2026-09-05。执行者：Codex AI。来源是用户提供的本地 PDF；没有将 AI 阅读写成人类专家复核。

## 完成范围

- 29 篇 PDF，337 页，已逐页查看原图并结合图注/相关正文完成第一轮阅读。
- 281 个图级记录：论文/文件/版本哈希、图号、PDF 页码、面板内容与编码、科学作用、迁移方式和边界。
- 12 张关键图另有细化卡片：布局比例、动作/数据接口、坐标或曲线、机制到证据、采集要求、可执行迁移方案，以及可编辑 SVG 布局草图。
- 每篇论文有整体图组故事及学习类型标签，避免只检索到“轨迹图”。29 篇按用户偏好成为 UAV 图形的优先来源；旧 100 篇统计仍只支持规模/角色，旧视觉案例作为补充。

入口：[优先范本索引](../references/preferred_29/INDEX.md)。本地浏览器入口：[原图与阅读笔记并排图库](../../USER_PREFERRED_VISUAL_LIBRARY/index.html)。原 PDF/页面/全文未放进可移植 Skill 包，派生阅读记录与草图在项目中。

## 关键改进

1. 明确允许主动模仿用户范本的分区、近似色系、图案、连线和面板关系；每张图必须说明借用和替换的内容。新增 [STYLE_PLAYBOOK](../references/preferred_29/STYLE_PLAYBOOK.md)。
2. 将策略输出分成高层决策与直接控制；显式区分捕获时间、速度约束、推力/角速度和姿态参考，不能套同一种 RL 框图。
3. 新增 [RL_FIGURE_PLAYBOOK](../references/preferred_29/RL_FIGURE_PLAYBOOK.md)：训练曲线、最终任务质量、安全、消融、泛化与实际闭环分别承担证据；记录步数/迭代/预算、奖励版本、随机种子和色带含义。
4. 本稿机制允许提出有理由的新设计；没有 C100 编号或完全匹配的参考图不再阻止规划。
5. 题目输入能产生具体图组、假设接口和草图。新增 [完整示例说明书](../examples/preferred_uav_rl/PAPER_FIGURE_DESCRIPTION.md)、[机制 SVG](../examples/preferred_uav_rl/mechanism.svg)、[事件综合图空布局](../examples/preferred_uav_rl/behavior_layout.svg)。示例不是用户已经完成的方法或结果。
6. 更新 SKILL、图形工作流、知识/范本路由、说明书模板、审计、网页版协议与默认提示。检索默认优先这 29 篇；精确图号可直接查询，未匹配时不强行塞入参考。

## 阅读中保留的差异与未知

- R02/R04 属相关研究线且有图形内容复用，不作为两次独立验证。
- R07 未在此版本定位到 Fig.2，未补造；R10/R14 正文提到 Fig.14，但阅读到的图止于 Fig.13。R09 的附录重新编号单列为 S1–S3。
- R15-F4 原图 y 轴与图注的 return 描述、归一化与色带定义需具体使用时再核验；没有擅自把阴影称为 95% CI。
- R20-F1 是 Blender 渲染任务示意。R20-F3 的共同初始集和原始奖励下评价是可迁移的重要比较原则。
- R25 的策略动作与 HOCBF 所用加速度之间存在图中未展开的转换；新稿应补接口。R25-F2 的训练成功阈值不能直接作为最终到达精度。
- R22 是神经估计/MPC，R27 是监督扩散，R24/R26/R28 是固定/零样本模型系统；没有误标为 RL。R27/R28 的所读实验为仿真，未称实机。
- 原图大部分以整页/联系表阅读；关键选图另看单页。281 条不是每个微小坐标、刻度和统计定义的全量转录。关键设计中的比例与色值标为观察估计/迁移近似。

## 验证与实际发现

- 来源、覆盖、图号、页范围、本地原页/正文链接及 29 个 PDF 哈希检查通过，见 [PREFERRED_29_VALIDATION.json](PREFERRED_29_VALIDATION.json)。构建器会拒绝把旧阅读记录静默套到变更后的 PDF 身份上。
- 检索行为已检查：`safety training` 首项 R25-F2；精确 R05-F4 可定位；无关系词不强制返回；未审核渲染不升级为可用参考；RL 查询不返回冻结/监督模型类条目。
- 本地浏览器测试：281 张图加载；R25-F1 精确筛选得到 1 项且原页成功显示；无匹配为 0；清空恢复 281；无脚本错误或页面水平溢出。
- 12 张布局学习图和 2 张示例图已渲染；查看布局联系表、选定原尺寸预览，并在 180 mm 对应的约 680 px 预览中查看两张示例。示例普通文字 20–22 SVG 单位，对应约 8.5–9.4 pt，底注约 7.65 pt。
- 视觉检查实际修复了：控制器文字溢出、训练梯度穿过标题、CairoSVG 对描边文字的渲染差异、两条布局摘要中不正确的反馈/训练动作连接；示例还补上约束构建所需的自机 p,v 来源。
- 原有结构检查 295/295、合成路由 9/9、旧案例来源验证及 Skill 快速校验通过。快速校验在 Windows 默认 GBK 下遇到解码错误，使用 `python -X utf8` 后通过。

## 尚未完成的质量证据

本轮的 12 张布局图是理解与安排面板的简图，不是原论文图的完整重绘。题目驱动示例是同一执行者制作/检查；没有真实作者手稿和实验素材，因此未完成新的真实手稿独立绘图交接，也没有实证结果图。结构检查不能认证科学主张或最终出版质量。后续具体手稿应从这套可追溯构图出发，补齐其真实接口与数据再验收。
