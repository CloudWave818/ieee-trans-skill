# VC-P001 · 把控制示例、超参数与误差分布组织成证据组

- 原论文：Distributionally Robust Chance Constrained Data-Enabled Predictive Control
- DOI：[10.1109/tac.2021.3097706](https://doi.org/10.1109/tac.2021.3097706)
- 范围：P001，PDF第9页，Fig. 2；仅以下图组，不代表全文已精读。
- 状态：`VISUAL_AND_CONTEXT_REVIEWED`；2026-09-05 Codex AI视觉检查，非人工复核。
- 原页：[打开本地原图](../../../IEEE_TRANS_VISUAL_LIBRARY/source_pages/P001_p09.png)；[邻页正文](../../../IEEE_TRANS_VISUAL_LIBRARY/source_pages/P001_p09_context.txt)。可移植包不含原页，缺失时用selection.json和prepare_visual_review.py从本地PDF重建。
- 样本范围：TAC / CORE / A；单例不代表期刊总体。
- 检索：control, sensitivity, distribution

## 原图观察

Fig.2 是双栏 2×3 面板。(a) 位置分量随秒变化，参考轨迹与红色约束线同轴；(b,c) 分别比较 Page/Hankel 结构的误差随 Wasserstein 半径变化，颜色对应 N；(d) 数据矩阵列数与误差；(e) 重叠误差直方图；(f) 初值窗口长度与误差。后五格不是同一轨迹的六个时间切片。

逐图检查所得面板计数（AI检查）：Fig.2 = 6。框图分组不等于独立子图，inset不另计主面板。

## 图注与正文说明

定位：PDF pp.9–10, V.B Choosing the Hyperparameters。

正文逐项检验半径、数据量、矩阵结构和初值窗口；(e) 只统计误差不超过200的273次仿真。图同时展示可工作实例与参数适用范围。

## 不可照搬与未确定项

原目录估计3个子图，原图实际6个。直方图有筛选条件，不能代表全部失败率；各面板实验总体不同，不能假定共享试次。不能把作者推测的噪声机制写成已证实因果。

## 可迁移设计判断

以下是针对后续手稿的设计建议，不是原论文结论：同一控制设计确有多个独立参数问题时，采用“行为实例→参数扫描→分布/窗口”组合；如果本稿只有性能主张，删掉无关参数格，不能按6格配额补图。

## 需要提前保存的实验/机制材料

保存每次仿真的矩阵类型、N、半径、列数、初值窗口、误差定义、成功/失败标签及全部试次；直方图过滤条件与被排除数量必须输出。

## 迁移布局草图

这是新的抽象布局说明，不是原图复制，也不包含本稿实验结果。

```text
[a 位置/参考/约束] [b 半径-误差 A] [c 半径-误差 B]
[d 样本量-误差]   [e 误差分布]    [f 窗口-误差]
```
